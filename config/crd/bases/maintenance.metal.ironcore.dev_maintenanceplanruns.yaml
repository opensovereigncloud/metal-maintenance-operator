---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: maintenanceplanruns.maintenance.metal.ironcore.dev
spec:
  group: maintenance.metal.ironcore.dev
  names:
    kind: MaintenancePlanRun
    listKind: MaintenancePlanRunList
    plural: maintenanceplanruns
    shortNames:
    - mplanrun
    singular: maintenanceplanrun
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.planRef.name
      name: Plan
      type: string
    - jsonPath: .spec.bmcRef.name
      name: BMC
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.currentStageIndex
      name: Stage
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MaintenancePlanRun is the Schema for the maintenanceplanruns
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MaintenancePlanRunSpec defines the input for a single BMC's
              maintenance run.
            properties:
              baselineBIOSVersions:
                additionalProperties:
                  type: string
                description: |-
                  BaselineBIOSVersions maps each server name to its BIOS firmware version
                  observed at run creation time. Used for per-server, per-stage version-aware
                  skip evaluation for Server-scoped stages.
                type: object
              baselineBMCVersion:
                description: |-
                  BaselineBMCVersion is the BMC firmware version observed at run creation time.
                  Used for per-stage version-aware skip evaluation for BMC-scoped stages.
                type: string
              bmcRef:
                description: |-
                  BMCRef is the BMC object that this run targets.
                  One run is created per unique BMC matched by the plan's serverSelector.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              planRef:
                description: PlanRef is the MaintenancePlan that generated this run.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              serverRefs:
                description: |-
                  ServerRefs are all Server objects that share this BMC.
                  BMC-scoped stages (BMCSettings/BMCVersion) execute once for the BMC.
                  Server-scoped stages (BIOSSettings/BIOSVersion) fan out one child CR per server.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                minItems: 1
                type: array
              stages:
                description: |-
                  Stages is a snapshot of the plan's stage list at run creation time.
                  Immutable after creation.
                items:
                  description: PlanStage defines a single ordered step in the maintenance
                    pipeline.
                  properties:
                    kind:
                      description: Kind is the type of metal-operator child CRD to
                        create for this stage.
                      enum:
                      - BMCSettings
                      - BMCVersion
                      - BIOSSettings
                      - BIOSVersion
                      type: string
                    name:
                      description: Name is the unique identifier for this stage within
                        the plan.
                      maxLength: 253
                      minLength: 1
                      type: string
                    template:
                      description: Template contains the spec payload for the child
                        CR.
                      properties:
                        biosSettings:
                          description: BIOSSettings is the template used when Kind
                            is BIOSSettings.
                          properties:
                            retryPolicy:
                              description: RetryPolicy defines the retry behavior
                                for automatic retries on transient failures.
                              properties:
                                maxAttempts:
                                  description: |-
                                    MaxAttempts is the maximum number of automatic retry attempts after failure.
                                    0 means no automatic retries. Must be between 0 and 10 inclusive.
                                    If not set, the operator-level default is used.
                                  format: int32
                                  maximum: 10
                                  minimum: 0
                                  type: integer
                              type: object
                            serverMaintenancePolicy:
                              description: ServerMaintenancePolicy is a maintenance
                                policy to be enforced on the server.
                              type: string
                            settingsFlow:
                              description: SettingsFlow contains the BIOS settings
                                sequence to apply in the given order.
                              items:
                                properties:
                                  name:
                                    description: Name is the name of the flow item.
                                    maxLength: 1000
                                    minLength: 1
                                    type: string
                                  priority:
                                    description: Priority defines the order of applying
                                      the settings. Lower numbers have higher priority
                                      (i.e. lower numbers are applied first).
                                    format: int32
                                    maximum: 2147483645
                                    minimum: 1
                                    type: integer
                                  settings:
                                    additionalProperties:
                                      type: string
                                    description: Settings contains software (e.g.
                                      BIOS, BMC) settings as a map.
                                    type: object
                                required:
                                - name
                                - priority
                                type: object
                              type: array
                            version:
                              description: Version specifies the software version
                                (e.g. BIOS, BMC) these settings apply to.
                              type: string
                          required:
                          - version
                          type: object
                        biosVersion:
                          description: BIOSVersion is the template used when Kind
                            is BIOSVersion.
                          properties:
                            image:
                              description: Image specifies the image to use to upgrade
                                to the given BIOS version.
                              properties:
                                URI:
                                  description: URI is the URI of the software image
                                    to install.
                                  type: string
                                secretRef:
                                  description: SecretRef is a reference to the Secret
                                    containing the credentials to access the image
                                    URI.
                                  properties:
                                    name:
                                      description: name is unique within a namespace
                                        to reference a secret resource.
                                      type: string
                                    namespace:
                                      description: namespace defines the space within
                                        which the secret name must be unique.
                                      type: string
                                  type: object
                                  x-kubernetes-map-type: atomic
                                transferProtocol:
                                  description: TransferProtocol is the network protocol
                                    used to retrieve the image URI.
                                  type: string
                              required:
                              - URI
                              type: object
                            retryPolicy:
                              description: RetryPolicy defines the retry behavior
                                for automatic retries on transient failures.
                              properties:
                                maxAttempts:
                                  description: |-
                                    MaxAttempts is the maximum number of automatic retry attempts after failure.
                                    0 means no automatic retries. Must be between 0 and 10 inclusive.
                                    If not set, the operator-level default is used.
                                  format: int32
                                  maximum: 10
                                  minimum: 0
                                  type: integer
                              type: object
                            serverMaintenancePolicy:
                              description: ServerMaintenancePolicy is a maintenance
                                policy to be enforced on the server.
                              type: string
                            updatePolicy:
                              description: UpdatePolicy indicates whether the server's
                                upgrade service should bypass vendor update policies.
                              type: string
                            version:
                              description: Version specifies the BIOS version to upgrade
                                to.
                              type: string
                          required:
                          - image
                          - version
                          type: object
                        bmcSettings:
                          description: BMCSettings is the template used when Kind
                            is BMCSettings.
                          properties:
                            retryPolicy:
                              description: RetryPolicy defines automatic retry behaviour
                                on transient failures.
                              properties:
                                maxAttempts:
                                  description: |-
                                    MaxAttempts is the maximum number of automatic retry attempts after failure.
                                    0 means no automatic retries. Must be between 0 and 10 inclusive.
                                    If not set, the operator-level default is used.
                                  format: int32
                                  maximum: 10
                                  minimum: 0
                                  type: integer
                              type: object
                            serverMaintenancePolicy:
                              description: ServerMaintenancePolicy is the maintenance
                                policy to apply on affected servers.
                              type: string
                            settings:
                              additionalProperties:
                                type: string
                              description: Settings contains BMC settings as a key/value
                                map.
                              type: object
                            version:
                              description: Version specifies the BMC firmware version
                                for which the settings apply.
                              type: string
                          required:
                          - version
                          type: object
                        bmcVersion:
                          description: BMCVersion is the template used when Kind is
                            BMCVersion.
                          properties:
                            image:
                              description: Image specifies the image to use to upgrade
                                to the given BMC version.
                              properties:
                                URI:
                                  description: URI is the URI of the software image
                                    to install.
                                  type: string
                                secretRef:
                                  description: SecretRef is a reference to the Secret
                                    containing the credentials to access the image
                                    URI.
                                  properties:
                                    name:
                                      description: name is unique within a namespace
                                        to reference a secret resource.
                                      type: string
                                    namespace:
                                      description: namespace defines the space within
                                        which the secret name must be unique.
                                      type: string
                                  type: object
                                  x-kubernetes-map-type: atomic
                                transferProtocol:
                                  description: TransferProtocol is the network protocol
                                    used to retrieve the image URI.
                                  type: string
                              required:
                              - URI
                              type: object
                            retryPolicy:
                              description: RetryPolicy defines the retry behavior
                                for automatic retries on transient failures.
                              properties:
                                maxAttempts:
                                  description: |-
                                    MaxAttempts is the maximum number of automatic retry attempts after failure.
                                    0 means no automatic retries. Must be between 0 and 10 inclusive.
                                    If not set, the operator-level default is used.
                                  format: int32
                                  maximum: 10
                                  minimum: 0
                                  type: integer
                              type: object
                            serverMaintenancePolicy:
                              description: ServerMaintenancePolicy is a maintenance
                                policy to be enforced on the server managed by referred
                                BMC.
                              type: string
                            updatePolicy:
                              description: UpdatePolicy indicates whether the server's
                                upgrade service should bypass vendor update policies.
                              type: string
                            version:
                              description: Version specifies the BMC version to upgrade
                                to.
                              type: string
                          required:
                          - image
                          - version
                          type: object
                      type: object
                  required:
                  - kind
                  - name
                  - template
                  type: object
                minItems: 1
                type: array
              trigger:
                default: Initial
                description: Trigger records why this run was created.
                enum:
                - Initial
                type: string
            required:
            - bmcRef
            - planRef
            - serverRefs
            - stages
            type: object
          status:
            description: MaintenancePlanRunStatus defines the observed state of MaintenancePlanRun.
            properties:
              completionTime:
                description: CompletionTime is when the run reached a terminal phase.
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the run's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentStageIndex:
                description: CurrentStageIndex is the zero-based index of the stage
                  currently being executed.
                format: int32
                type: integer
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: Phase is the high-level state of this run.
                enum:
                - Pending
                - Running
                - Succeeded
                - Failed
                type: string
              stageStatuses:
                description: StageStatuses holds per-stage execution state, indexed
                  in the same order as Spec.Stages.
                items:
                  description: StageStatus captures the observed state of one stage
                    in a run.
                  properties:
                    appliedSpec:
                      description: |-
                        AppliedSpec is a snapshot of the child CR's spec at the time it completed.
                        Populated before intermediate-hop CRs are deleted so the run retains a full audit record.
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    childRef:
                      description: |-
                        ChildRef is the object reference to the child CR for BMC-scoped stages (BMCSettings/BMCVersion).
                        Empty for Server-scoped stages; see ServerStatuses instead.
                      properties:
                        apiVersion:
                          description: API version of the referent.
                          type: string
                        fieldPath:
                          description: |-
                            If referring to a piece of an object instead of an entire object, this string
                            should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                            For example, if the object reference is to a container within a pod, this would take on a value like:
                            "spec.containers{name}" (where "name" refers to the name of the container that triggered
                            the event) or if no container name is specified "spec.containers[2]" (container with
                            index 2 in this pod). This syntax is chosen only to have some well-defined way of
                            referencing a part of an object.
                          type: string
                        kind:
                          description: |-
                            Kind of the referent.
                            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                          type: string
                        name:
                          description: |-
                            Name of the referent.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                        namespace:
                          description: |-
                            Namespace of the referent.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                          type: string
                        resourceVersion:
                          description: |-
                            Specific resourceVersion to which this reference is made, if any.
                            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                          type: string
                        uid:
                          description: |-
                            UID of the referent.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    completionTime:
                      description: CompletionTime is when this stage reached a terminal
                        phase.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human-readable description of the
                        current stage state.
                      type: string
                    name:
                      description: Name matches the stage name from MaintenancePlanSpec.Stages.
                      type: string
                    phase:
                      description: Phase is the current execution phase of this stage.
                      enum:
                      - Pending
                      - Running
                      - Skipped
                      - Succeeded
                      - Failed
                      type: string
                    serverStatuses:
                      description: |-
                        ServerStatuses holds per-server execution state for Server-scoped stages
                        (BIOSSettings/BIOSVersion). Empty for BMC-scoped stages.
                      items:
                        description: ServerStageStatus captures the execution state
                          for one server within a Server-scoped stage.
                        properties:
                          childRef:
                            description: ChildRef is the object reference to the child
                              CR created for this server (if any).
                            properties:
                              apiVersion:
                                description: API version of the referent.
                                type: string
                              fieldPath:
                                description: |-
                                  If referring to a piece of an object instead of an entire object, this string
                                  should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                                  For example, if the object reference is to a container within a pod, this would take on a value like:
                                  "spec.containers{name}" (where "name" refers to the name of the container that triggered
                                  the event) or if no container name is specified "spec.containers[2]" (container with
                                  index 2 in this pod). This syntax is chosen only to have some well-defined way of
                                  referencing a part of an object.
                                type: string
                              kind:
                                description: |-
                                  Kind of the referent.
                                  More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                                type: string
                              name:
                                description: |-
                                  Name of the referent.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                              namespace:
                                description: |-
                                  Namespace of the referent.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                                type: string
                              resourceVersion:
                                description: |-
                                  Specific resourceVersion to which this reference is made, if any.
                                  More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                                type: string
                              uid:
                                description: |-
                                  UID of the referent.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                                type: string
                            type: object
                            x-kubernetes-map-type: atomic
                          completionTime:
                            description: CompletionTime is when this server's child
                              CR reached a terminal phase.
                            format: date-time
                            type: string
                          message:
                            description: Message is a human-readable description of
                              the current state for this server.
                            type: string
                          phase:
                            description: Phase is the current execution phase for
                              this server's child CR.
                            enum:
                            - Pending
                            - Running
                            - Skipped
                            - Succeeded
                            - Failed
                            type: string
                          serverRef:
                            description: ServerRef identifies the server this status
                              entry belongs to.
                            properties:
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                            type: object
                            x-kubernetes-map-type: atomic
                          startTime:
                            description: StartTime is when the child CR was created
                              for this server.
                            format: date-time
                            type: string
                        required:
                        - phase
                        - serverRef
                        type: object
                      type: array
                    startTime:
                      description: StartTime is when this stage began executing.
                      format: date-time
                      type: string
                  required:
                  - name
                  - phase
                  type: object
                type: array
              startTime:
                description: StartTime is when the run transitioned from Pending to
                  Running.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
